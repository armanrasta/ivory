{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ivory.serviceAccountName" . }}
  labels:
    {{- include "ivory.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
